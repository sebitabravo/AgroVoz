[general]
static=yes
writeprotect=yes
clearglobalvars=yes

; Flujo local mínimo de respaldo (#172).
; No expone SIP/PSTN: se prueba con un canal Local desde la CLI de Asterisk.
[agrovoz-local]
exten => 600,1,Answer()
 same => n,Wait(1)
 same => n,Playback(agrovoz/precio-papa)
 same => n,Hangup()
