[general]
; Habilita la interfaz REST/WebSocket de ARI (C10, #172). Solo escucha
; dentro de la red interna de Docker Compose: el servicio "ivr" no publica
; el puerto 8088 al host ni a Internet (ver docker-compose.yml).
enabled=yes
bindaddr=0.0.0.0
bindport=8088
